import { historical_event_desc } from './descriptions-main.js';

const narrate = (historicalEvent, dwarfViz) => {
  console.log(historical_event_desc(historicalEvent, dwarfViz));
  return historical_event_desc(historicalEvent, dwarfViz);
};

export default narrate;
