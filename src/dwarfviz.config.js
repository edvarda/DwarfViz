const config = {
  useStaticData: true, // set to true to only load static data files
  storytellerURL: 'https://api.dwarfviz.com/api', // address for dwarf storyteller apiserver
};
export default config;
