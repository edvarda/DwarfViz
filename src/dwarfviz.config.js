const config = {
  useStaticData: true, // set to true to only load static data files
  storytellerURL: 'http://178.62.223.248/api', // address for dwarf storyteller apiserver
};
export default config;
